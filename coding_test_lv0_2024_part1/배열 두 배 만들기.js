function solution(numbers) {
  let answer = numbers.map((item) => item * 2);

  // let answer=[];
  // for (let i=0; i<=(numbers.length-1); i++) {
  //     newNumbers = numbers[i] * 2
  //     answer.push(newNumbers)
  // }

  return answer;
}
